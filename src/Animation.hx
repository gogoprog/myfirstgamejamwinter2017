
class Animation
{
    public var line:Int;
    public var length:Int;
    public var framerate:Int;

    public function new(line, length, framerate)
    {
        this.line = line;
        this.length = length;
        this.framerate = framerate;
    }
}
